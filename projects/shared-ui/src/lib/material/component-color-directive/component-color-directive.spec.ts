import { ComponentColorDirective } from './component-color-directive';

describe('ComponentColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentColorDirective();
    expect(directive).toBeTruthy();
  });
});
