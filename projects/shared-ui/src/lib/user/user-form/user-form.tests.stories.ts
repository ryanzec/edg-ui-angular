import type { Meta } from '@storybook/angular';
import { UserForm } from './user-form';

const meta: Meta<UserForm> = {
  title: 'User/Components/User Form/Tests',
  component: UserForm,
};

export default meta;
