import type { Meta } from '@storybook/angular';
import { UserDeleteDialog } from './user-delete-dialog';

const meta: Meta<UserDeleteDialog> = {
  title: 'User/Components/User Delete Dialog/Tests',
  component: UserDeleteDialog,
};

export default meta;
