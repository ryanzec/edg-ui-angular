import type { Meta } from '@storybook/angular';
import { ScrollAreaDirective } from './scroll-area-directive';

const meta: Meta = {
  title: 'Core/Directives/Scroll Area/Tests',
  component: ScrollAreaDirective,
};

export default meta;

// test stories will be added here
