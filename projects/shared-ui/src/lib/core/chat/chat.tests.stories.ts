import type { Meta } from '@storybook/angular';
import { Chat } from './chat';

const meta: Meta<Chat> = {
  title: 'Core/Components/Chat/Tests',
  component: Chat,
};

export default meta;

// test stories will be added here
