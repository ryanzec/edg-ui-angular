import type { Meta } from '@storybook/angular';
import { Markdown } from './markdown';

const meta: Meta<Markdown> = {
  title: 'Core/Components/Markdown/Tests',
  component: Markdown,
};

export default meta;

// test stories will be added here
