import type { Meta } from '@storybook/angular';
import { DateDisplay } from './date-display';

const meta: Meta<DateDisplay> = {
  title: 'Core/Components/Date Display/Tests',
  component: DateDisplay,
};

export default meta;

// test stories will be added here
