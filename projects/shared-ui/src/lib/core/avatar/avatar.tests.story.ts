import type { Meta } from '@storybook/angular';
import { Avatar } from './avatar';

const meta: Meta<Avatar> = {
  title: 'Core/Avatar/Tests',
  component: Avatar,
  parameters: {
    layout: 'centered',
  },
};

export default meta;

// Add test stories here as needed
