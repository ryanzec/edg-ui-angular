import type { Meta } from '@storybook/angular';
import { OverlayMenu } from './overlay-menu';

const meta: Meta<OverlayMenu> = {
  title: 'Core/Components/Overlay Menu/Tests',
  component: OverlayMenu,
};

export default meta;

// play/test stories to be implemented
