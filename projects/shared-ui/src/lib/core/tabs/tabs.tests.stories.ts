import type { Meta } from '@storybook/angular';
import { Tabs } from './tabs';

const meta: Meta<Tabs> = {
  title: 'Core/Components/Tabs/Tests',
  component: Tabs,
};

export default meta;

// test stories to be implemented
