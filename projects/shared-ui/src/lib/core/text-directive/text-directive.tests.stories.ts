import type { Meta } from '@storybook/angular';
import { TextDirective } from './text-directive';

const meta: Meta = {
  title: 'Core/Directives/Text/Tests',
  component: TextDirective,
};

export default meta;

// test stories will be added here
