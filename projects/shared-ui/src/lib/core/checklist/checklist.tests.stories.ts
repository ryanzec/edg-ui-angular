import type { Meta } from '@storybook/angular';
import { Checklist } from './checklist';

const meta: Meta<Checklist> = {
  title: 'CoreComponents/Checklist/Tests',
  component: Checklist,
};

export default meta;

// test stories will be added here
