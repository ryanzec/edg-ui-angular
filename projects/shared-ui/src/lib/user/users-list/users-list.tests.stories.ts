import type { Meta } from '@storybook/angular';
import { UsersList } from './users-list';

const meta: Meta<UsersList> = {
  title: 'User/Components/Users List/Tests',
  component: UsersList,
};

export default meta;

// Play/test stories will be added here
