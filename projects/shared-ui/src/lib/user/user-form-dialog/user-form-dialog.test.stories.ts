import { type Meta } from '@storybook/angular';
import { UserFormDialog } from './user-form-dialog';

const meta: Meta<UserFormDialog> = {
  title: 'User/Components/User Form Dialog Tests',
  component: UserFormDialog,
};

export default meta;
