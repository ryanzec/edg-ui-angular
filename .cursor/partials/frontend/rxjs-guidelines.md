RxJS Guidelines:
- Always use built-in functionality over creating custom when available
- Make sure error handling is robust
- Make sure streams properly composed to avoid nested subscriptions whenever possible
