import type { Meta } from '@storybook/angular';
import { CodeBlock } from './code-block';

const meta: Meta<CodeBlock> = {
  title: 'Core/CodeBlock/Tests',
  component: CodeBlock,
};

export default meta;
